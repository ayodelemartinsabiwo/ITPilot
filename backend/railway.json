{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m venv /opt/venv && /opt/venv/bin/pip install --upgrade pip && /opt/venv/bin/pip install -r requirements.txt && /opt/venv/bin/python manage.py collectstatic --noinput && /opt/venv/bin/python manage.py migrate"
  },
  "deploy": {
    "startCommand": "bash -c '/opt/venv/bin/daphne -b 0.0.0.0 -p ${PORT} core.asgi:application'",
    "healthcheckPath": "/admin/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
